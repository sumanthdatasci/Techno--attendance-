<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techno.attendance">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Attendance">

        <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Member Management Activities -->
        <activity android:name=".activities.MembersListActivity" />
        <activity android:name=".activities.AddEditMemberActivity" />
        <activity android:name=".activities.MemberDetailsActivity" />

        <!-- Attendance Activity -->
        <activity android:name=".activities.AttendanceActivity" />

        <!-- Reports Activities -->
        <activity android:name=".activities.MonthlyReportActivity" />
        <activity android:name=".activities.AbsenteesReportActivity" />

        <!-- Settings Activities -->
        <activity android:name=".activities.SettingsActivity" />
        <activity android:name=".activities.HolidayManagementActivity" />
        <activity android:name=".activities.AboutActivity" />

        <!-- Backup & Restore Activity -->
        <activity android:name=".activities.BackupRestoreActivity" />

        <!-- FileProvider for sharing backup files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
